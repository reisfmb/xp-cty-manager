import { IComponentProps } from '../type';

export default {
  component: 'FieldOrItemSet',
  field: {
    label: 'Field Set',
    hint: 'A field set may be used to group items visually under a common label and distinguish this group from the rest of the form.',
  },
} as IComponentProps;
