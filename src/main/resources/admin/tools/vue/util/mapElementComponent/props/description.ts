import { IComponentProps } from '../type';

export default {
  component: 'TextInput',
  field: {
    label: 'Description',
    hint: 'Set a description that is shown when creating the content type.',
  },
  rules: [],
  i18n: true,
} as IComponentProps;
