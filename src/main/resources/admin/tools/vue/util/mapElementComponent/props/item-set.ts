import { IComponentProps } from '../type';

export default {
  component: 'FieldOrItemSet',
  field: {
    label: 'Item Set',
    hint: 'Item sets enable combining inputs (or other sets) into complex, nested structures. Item sets are grouped both visually and semantically, as the name of the item set is used in the persisted property structure.',
  },
} as IComponentProps;
