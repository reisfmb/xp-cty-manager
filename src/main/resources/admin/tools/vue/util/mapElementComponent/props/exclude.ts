import { IComponentProps } from "../type";

export default {
  component: "TextInput",
  field: {
    label: "Exclude",
    hint: "Optionally removes the standard tools from the editor toolbar.",
  },
  rules: [],
  i18n: false,
} as IComponentProps;
