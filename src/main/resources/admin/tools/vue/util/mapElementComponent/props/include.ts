import { IComponentProps } from "../type";

export default {
  component: "TextInput",
  field: {
    label: "Include",
    hint: "Optionally adds more tools to the editor toolbar.",
  },
  rules: [],
  i18n: false,
} as IComponentProps;
